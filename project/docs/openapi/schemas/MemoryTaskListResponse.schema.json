{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "MemoryTaskListResponse",
  "type": "object",
  "description": "Response body for `GET /memory/tasks` containing a list of task snapshots.",
  "required": [
    "source",
    "items"
  ],
  "properties": {
    "source": {
      "type": "string",
      "description": "Backend that served the results (`arcadedb` or `registry`).",
      "enum": [
        "arcadedb",
        "registry"
      ]
    },
    "items": {
      "type": "array",
      "items": {
        "$ref": "TaskSnapshot.schema.json"
      }
    }
  }
}
